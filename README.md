LÖVE starfield in ~100 lines of lua
===================================

Just horsing around with LÖVE a bit. You need to get it from [here](https://love2d.org). After, you can run this demo with `love [path-to-repo]`.
